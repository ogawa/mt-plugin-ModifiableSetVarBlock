# $Id$
package ModifiableSetVarBlock::L10N::ja;

use strict;
use base 'ModifiableSetVarBlock::L10N';
use vars qw( %Lexicon );

our %Lexicon = (
    q(ModifiableSetVarBlock overrides MTSetVarBlock with an alternative version, which can be used with 'modifiers'.) => q(ModifiableSetVarBlockは、MTSetVarBlockでmodifierを利用可能にします。),
);

1;
